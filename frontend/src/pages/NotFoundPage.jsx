import React from 'react';
import NotFound from '@features/NotFoundPage/NotFoundPage.jsx';

function NotFoundPage() {
    return (
        <>
            <NotFound />
        </>
    );
}

export default NotFoundPage;
